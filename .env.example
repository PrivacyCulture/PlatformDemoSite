REPLICATE_API_TOKEN=your_token_here

# Replicate model version string, e.g. "stability-ai/stable-video-diffusion:3f0457..." or similar.
# You MUST set this to a model that outputs an MP4 (or a URL to one).
REPLICATE_MODEL=
